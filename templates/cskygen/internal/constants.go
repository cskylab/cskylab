package internal

// tplConfigFile: Template config file name (Must exist as *.yaml in template directory)
const tplConfigFile = "_values-tpl"

// viperConfigFile: viper config file name
const viperConfigFile = "_tpl-overrides"

// msgInfo contains string with info message header
const msgInfo = "\033[0;32mInfo:\033[0m" // Green

// confirmationMsg contains prompt confirmation message
const confirmationMsg = "Press Enter to continue or Ctrl+C to abort"
